# Copy this file to .env and fill in the values before running locally.
# On Railway, set these as environment variables in the service settings.

PORT=3000

# Secret key used to protect the /api/config and /api/conversations endpoints.
# Choose a long random string — treat it like a password.
ADMIN_KEY=change-me-to-a-long-random-secret
